const fs = require('fs'); let c = fs.readFileSync('../frontend/src/apps/admin/components/structure/StructureImport.tsx', 'utf8'); c = c.replace(/const \[loading, setLoading\] = useState\\(false\\);/, 'const [loading, setLoading] = useState(false);\\n    const [uploadProgress, setUploadProgress] = useState(0);\\n    const [importStatus, setImportStatus] = useState("Waiting for upload...");'); fs.writeFileSync('../frontend/src/apps/admin/components/structure/StructureImport.tsx', c);
